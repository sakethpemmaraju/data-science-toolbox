# Core (required by all notebooks)
# pandas pinned below 3.0: the pandas-3 default string dtype changes
# object-dtype behavior; the notebooks are written for the pandas 2.x API.
pandas>=2.0,<3.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
scipy>=1.10
statsmodels>=0.14
joblib>=1.3
sqlalchemy>=2.0
openpyxl>=3.1

# Modeling extras
# Pinned: newer xgboost wheels require a newer libomp than Anaconda ships on macOS.
xgboost==2.0.3
shap>=0.44

# Optional
# lightgbm>=4.0        # extra model in notebook 08
# psycopg2-binary      # PostgreSQL access
# pymysql              # MySQL access
# pyodbc               # SQL Server access
